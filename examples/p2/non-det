// NOT DETERMINISTIC
// OUTPUTS: x = alpha(-2) U alpha(2)
if [0,1] == 0 then {
    x:= [-2,-2];
}else {
    x:= 2;
}    
// Examples:
// Int (no bound) -> [-2,2]
// Int [0,10]     -> [-inf,2]
// Sign           -> T
// Ext Sign       -> !=0
// Cong           -> 4Z+2